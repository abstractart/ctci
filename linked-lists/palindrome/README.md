# Список - палиндром
```python3
def palindromeList(head):
  l = length(head)
  middle = l // 2
  
  return False
  
def length(head):
  l = 0
  while(head):
    l += 1
    head = head.next

  return l
```
